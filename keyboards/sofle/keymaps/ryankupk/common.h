#pragma once

enum sofle_layers {
    _DEFAULTS = 0,
    _QWERTY = 0,
    _COLEMAKDH,
    _GAMING,
    _LOWER,
    _RAISE,
    _ADJUST
};

enum custom_keycodes {
    KC_QWERTY = SAFE_RANGE,
    KC_CLMKDH,
    KC_GAMING,
    KC_LOWER,
    KC_RAISE,
    KC_ADJUST
};
